# Patches — every fork change that touches an upstream file

New code lives in `Sources/Search/Fork/` and never appears here. This table is only the lines in upstream's files, so a rebase knows what to protect. Update it in the same commit as the change; delete a row when its "drop when" fires.

| patch | touches | why | upstream status | drop when |
|---|---|---|---|---|
| updater-off | `Updater.swift` `checkIfDue()` guard | a Copper build must not replace itself with upstream Search | fork-only | Copper has its own feed + signing identity (then repoint, keep the guard) |
| app-identity | `Store.swift` folder + `ownContainer`, `Vault.swift` label, `build.sh` NAME/APP/bundle id, `Icon/icon.swift` + `Icon/plate.jpg` (Copper's own icon), `CHANGELOG.md` "Added (Copper)" block at the top | coexist with upstream Search: separate Application Support, keychain items, WebKit container | fork-only | never |
| branding | `App.swift`, `Browser.swift`, `Design.swift`, `ExtensionShims.swift`, `Extensions.swift`, `ExtensionsUI.swift`, `Links.swift`, `Passwords.swift`, `Settings.swift`, `StoreRelay.swift`, `Updater.swift`, `Welcome.swift`, `build.sh`, `Installer/background.swift`, `bench` — user-visible strings plus the About/Welcome marks | Copper is the product; Search is the engine's lineage | fork-only | never |
| spaces-hooks | `Browser.swift` `tabs` setter opened, `prepare` opened, init restore → `Spaces.restore`, `writeSession` → `Spaces.shape`; `Session.swift` optional `space`/`active` on Entry, `spaces`/`space` on Shape; `Side.swift` `SpaceStrip` above foot; `App.swift` `SpaceCommands`; `Sleep.swift` idle set includes parked tabs; `Bench.swift` `spaces` verb; `bench` folder name + verb | Spaces (FORK-PLAN #1) | fork-only (upstream is one-row by design) | never |
| profiles-hook | `Store.swift` `websites` asks `Spaces.profileStore` first | per-space cookie jars (FORK-PLAN #2) | fork-only | never |
| command-bar-hooks | `Browser.swift` `guess()` summon branch → `CommandBar.offers`; `submit()` picks in other spaces via `Spaces.reveal`, runs `copper://command/*` via `CommandBar.run`; `History.swift` `Suggestion.Kind` + `.bookmark`, `.command`; `Omnibox.swift` two icon cases; `Bench.swift` `bar` verb (via `Fork.bench`) | ⌘K command bar (FORK-PLAN #4) | fork-only | never |
| command-bar-card | `Omnibox.swift` `body` sends `browser.summoning` to `Fork/CommandPalette.swift` (⌘L keeps upstream's field, unchanged, as `plain`); `AddressField` gains `literal`/`size`/`placeholder` — `literal` shows what was typed instead of the completed address and leaves the caret at the end, so walking the list does not rewrite the question; `History.swift` `Suggestion` gains optional `hint`, `detail`, `glyph`, `badge` (additive, defaulted) | ⌘K as a floating card, Arc's ⌘T: 640pt, big input, icon rows, right-side hints (gauntlet: commandbar) | fork-only | never |
| host-marks | no upstream file edited — `Fork/Marks.swift` calls upstream's existing `Favicons.adopt(_:for:)` for hosts it fetched itself (the sidebar's `Marks.warm(tabs)` and the command bar's `Marks.want(urls)`; the two builders' fetchers were folded into this one at smoothing) | upstream's `Favicons.fetch` only works `for tab:`, through a live WKWebView, so a sleeping tab, a page in another space, a bookmark or a history row could never get a site mark and fell to a letter tile. Marks asks without a page: Arc's own icon cache first, then `/favicon.ico`, the homepage's `<link rel=icon>` tags, `/apple-touch-icon.png`, then the parent domain's `/favicon.ico`, and hands the bytes to `adopt`, which decodes, squares, writes to `icons/` and fires `arrived` — so the mark reaches the tabs, the sidebar and the ⌘K rows alike. **Rebase note:** `icons/` and `Favicons.arrived` now get written without a web view in the loop | fork-only | upstream grows a host-level fetch of its own (then delete `Marks.fetch` and call it) |
| split-hooks | `App.swift` `Page(tab:)` → `SplitStage`, `SpaceCommands` → `ForkCommands` (one Commands for everything Copper adds; the builder caps at ten); `Tab.swift` `onTouch` also calls `Tab.touched`; `Browser.swift` wires `Tab.touched` at launch and calls `Split.arriving` in `select(_:)` so the panes trade places in the same breath as the selection — doing it after, off the stage's `onChange`, leaves a frame with the same page in both panes and one of them ends up blank; `Sleep.swift` side pane counts as on screen; `Bench.swift` `split` verb | Split view (FORK-PLAN #3) | fork-only | never |
| bench-window | `Bench.swift` `summon`, `window` verbs (via `Fork.bench`): ⌘K left open with text; whole-window PNG for the gauntlet critic | Gauntlet loop screenshots | fork-only | never |
| arc-sidebar | `Side.swift` reshaped: the overflowing `ZStack` becomes a `VStack` washed in `SpaceTint`, favicons are always on in this one view (upstream's `prefs.glyph == .icons` gate dropped), the favourites grid is capped at 3–4 columns with Arc's wide short cells, the loose rows split into two scrolls under a soft fade — carried-over above, today below — either side of a hairline and a "New Tab" row, resting titles sit at `tint.ink.opacity(0.85)` rather than a muted grey, the bookmark/extension doors move onto the space strip's own row, and `Quiet` gains `inset`/`glow`/`ink`/`fill` (the New Tab seam row wears the favourites' square at rest, as Arc's does) plus `Door` a `size` so a tinted column can hand its own colours and sizes in, `RowMark` non-private so the ⌘K rows draw the same icon/letter chip | Arc-grade sidebar (gauntlet: sidebar) | fork-only (upstream's sidebar has no spaces to tint) | never |
| groups-hooks | `Side.swift` `loose` → `GroupedRows` (upstream's VStack kept as `looseUngrouped`), `rowsEnd` adds `GroupedRows.extraRows`, `SideRow` made non-private; `TabBar.swift` `TabMenu` ends with `GroupMenu`; `Session.swift` optional `group` on Entry; `Browser.swift` `didFinish` → `Grouper.landed`, `close` → `Grouper.forget`, launch → `Updates.shared.start`; `Fork/Spaces.swift` `ForkCommands` carries `GroupCommands` (App.swift builder at its cap); `Settings.swift` `Page` gains `intelligence`, `agents`, `updates` and routes the Updates page; `Bench.swift` `groups`/`ai`/`updates` verbs (via `Fork.bench`); `bench` verbs | Tab groups with Jev/router-assisted grouping, drawn as Arc's nested folders (FORK-PLAN #5, "folders", done as groups; the nesting is read out of the ` › ` names by `Fork/Folders.swift`, so no upstream file learns about it); Copper's feed-owned Updates page and command hook | fork-only | never |
| mcp-hooks | `Browser.swift` init → `MCP.shared.start`; `App.swift` `SearchApp.init` → `Bridge.runIfAsked` (`--mcp-stdio`, `--cli`); `Settings.swift` Agents page (row shared with groups-hooks); `Bench.swift` `agent` verb (via `Fork.bench`) | Copper as an MCP server: agents drive the open window (FORK-PLAN Part 1b C, server half) | fork-only | never |
| session-guard | `Session.swift` write delegates to `Fork/SessionGuard`; `Fork/Spaces.swift` restore brackets the restored flag | Refuse launch-time empty session overwrites, rotate `session.previous.json` on destructive drops, and keep writes atomic | fork-only | never |
| sections-hooks | `Side.swift` `carried`/`today` become `saved`/`today` off `Sections.isSaved` (block offsets + the seam-crossing drag, a `ScrollViewReader` that shows a newly saved row), two `onChange` hooks (`arrived`, `note`); `Session.swift` optional `saved`/`seen` on Entry; `TabBar.swift` `TabMenu` gains `SaveMenu`; `Settings.swift` Tabs card gains `ArchiveLine`; `Bench.swift` `sections` verb (via `Fork.bench`); `Fork/Spaces.swift` `carried` set replaced by `Sections` (shape writes the two fields, restore reads them, `select` sweeps the row it lands on); `bench` verb; `arc-import` writes `saved`/`seen` | Arc's three sidebar sections — Favourites, Saved, Today, with Today archived after a day through the normal close path | fork-only | never |
| space-swipe | `Browser.swift` `SpaceSwipe.watch(self)` next to the `Tab.touched` wiring; `Bench.swift` `swipe` verb (via `Fork.bench`) | Two-finger swipe over the sidebar switches space (`Fork/Swipes.swift`) | fork-only | never |
| heat-hooks | `Browser.swift:718` (`override init()` defer, `Heat.shared.start(for: self)`); `Side.swift:488` (`SideRow` observes Heat) and `536-541` (status `ZStack` branch after `tab.noisy`); `Bench.swift:234` (`handle` dispatch list adds `heat`); no `TabBar.swift` change | Show an ember for a built tab sustaining high WebContent CPU, with a CPU tooltip and `bench heat` report | fork-only | never |
| keychain-lazy-secrets | `Vault.swift` `login(from:)` lists without secrets, `secret(host:user:)` public, `resolve(_:)`, `touch` updates the comment only; `Browser.swift` `choose`/`copy`/`onCredentials` resolve one account at a time; `Passwords.swift` `Account` reads the secret on Show | drawing the account list read every item's secret, and each item saved by an earlier ad-hoc build made the keychain ask — one dialog per item, on every click into a sign-in box (Google sign-in was unusable) | fork-only; worth upstreaming | upstream lists without reading secrets |
| credentials-browser | `Browser.swift` `Offer`, `Suggesting`, `fetching`, `keepOffer()`, `choose(_:)`, `Suggestion`, `rows`, `choose(Suggestion)`, `onField`, `onCredentials`; `Accounts.swift` `AccountList`/`Row` kinds | Bitwarden credentials plus identity/card/field/username suggestions and agent sign-in (docs/plans/2026-09-27-bitwarden-autofill-everything.md) | fork-only | never |
| credentials-forms | `Forms.swift` `FormRelay` `__officeForms` classify/fillValues/fillFocused/fillField/hasFields/fieldsPresent (plus submit/OTP); `Tab.swift` FocusedField/fieldFocus/fieldFocused/fillValues/fillFocused/fillField/hasFields/fieldsPresent | Autofill identities, cards, custom fields, and usernames plus Bitwarden credentials (docs/plans/2026-09-27-bitwarden-autofill-everything.md) | fork-only | never |
| credentials-settings | `Prefs.swift` `Prefs.fillsEverything`; `Settings.swift` Passwords `Fill addresses and cards`; `SettingsPanel` Passwords page mount + status refresh | Bitwarden autofill-everything switch and counts (docs/plans/2026-09-27-bitwarden-autofill-everything.md) | fork-only | never |
| credentials-bench | `Fork.swift` `bw` identities/cards/fields/usernames/counts/autofill/share dispatch; `bench` `main()` help/parser/output for `bw` | Bitwarden autofill-everything probe verbs (docs/plans/2026-09-27-bitwarden-autofill-everything.md) | fork-only | never |
| flow-core | `Import.swift` key/read overload + shared Chromium crypto helpers; `Fork/Spaces.swift` adopt API; `Fork/Split.swift` sheet hook | Flow import merge and isolated sheet | fork-only | never |
| flow-hooks | `Fork/CommandBar.swift`, `Settings.swift`, `Bench.swift`, `Fork/Fork.swift` | Copper commands, Settings rows, and bench routing, including `passkeys` | fork-only | never |
| passkeys-hooks | `Tab.swift`, `Settings.swift`, `App.swift`, `Bench.swift` | Copper's page-side WebAuthn authenticator, unentitled preference migration, Settings card, and test-world bench verbs; native implementation stays in `Fork/Passkeys/` | fork-only | when upstream exposes WebAuthn to third-party builds |
| chromium-key-visibility | `Import.swift` `safeStorage`, `stretch`, `unwrap` visibility | Let Fork cookie import reuse Chromium's existing keychain, PBKDF2, and AES helpers so passwords and cookies share one key | fork-only; not upstream | when upstream exposes these helpers (then remove this row and the visibility patch) |
| headless | no upstream file edited — `Fork/Headless.swift` (booted from `Fork/MCP/Bridge.swift`, finished from `Fork/MCP/MCP.swift` `start(for:)`) swizzles, **only when `--headless`/`SEARCH_HEADLESS=1`**, AppKit entry points upstream code calls: `NSWindow` `makeKeyAndOrderFront:`/`orderFront:`/`orderFrontRegardless`/`orderWindow:relativeTo:`/`constrainFrameRect:toScreen:`/`setFrameAutosaveName:`, `WKWebView` `viewDidMoveToWindow` (sets WebKit SPI `_setWindowOcclusionDetectionEnabled:` NO), `NSApplication` `activateIgnoringOtherApps:`/`activate`/`runModalForWindow:`, `NSAlert` + `NSSavePanel`/`NSOpenPanel` `runModal`/`beginSheetModalForWindow:completionHandler:`; observes `Browser.$welcoming`/`$asking` | headless Copper for the grunts Mac mini (docs/headless.md): no Dock icon, window parked off every display, no activation, every modal declined. **Rebase note:** upstream's `Dialogs.show`, `Links.hand`, `Browser` floater `onReturn`, `Extensions` alerts and `ContentView.dress` (`setFrameAutosaveName`) are covered by these interceptions rather than hooks — a new upstream modal/activation path needs no hook, but one that uses a different AppKit API (e.g. `NSPrintOperation`) is not declined | fork-only | never |
